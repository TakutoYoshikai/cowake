{
  "name": "cowake",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "homepage": "https://cowake.yoshikai.net",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npm run build && gh-pages -d . -m \"Updates --skip-ci\"",
    "build": "npm install && babel src -d build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "csv-parse": "^7.0.2",
    "express": "^4.22.3",
    "papaparse": "^5.7.0",
    "tablesort": "^5.7.1"
  },
  "devDependencies": {
    "@babel/cli": "^8.0.5",
    "@babel/core": "^8.0.5",
    "@babel/preset-env": "^8.0.5",
    "@babel/preset-react": "^8.0.1",
    "gh-pages": "^6.3.0"
  }
}
